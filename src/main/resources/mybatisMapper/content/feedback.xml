<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dgjs.mapper.content.FeedBackMapper">
  
  	<insert id="save" parameterType="feedBack" keyProperty="id" useGeneratedKeys="true">
	       insert into feedback
	       (
	           articlescrap_id,
	           ip,
	           judge_level,
	           judge_message,
	           uname,
	           email,
	           doubt_level,
	           create_time
	       )
	       values
	       (
	           #{articlescrap_id},
	           #{ip},
	           #{judge_level},
	           #{judge_message},
	           #{uname},
	           #{email},
	           #{doubt_level},
	           now()
	       )
	</insert>
	
	<select id="getLevelCount" resultType="int">
	    select count(1) from feedback where articlescrap_id=#{articlescrap_id}
	    <if test="judge_level!=null">and judge_level=#{judge_level}</if>
	</select>
	
</mapper>
