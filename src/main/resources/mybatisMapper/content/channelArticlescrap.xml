<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dgjs.mapper.content.ChannelArticlescrapMapper">
  
  	<insert id="save" parameterType="channelArticlescrap" keyProperty="id" >
	       insert into channel_articlescrap
	       (
	           id,
	           articlescrap_id,
	           channel_id,
	           sort
	       )
	       values
	       (
	           #{id},
	           #{articlescrap_id},
	           #{channel_id},
	           #{sort}
	       )
	</insert>
	
	<update id="update">
	       update channel_articlescrap set 
	           sort=#{sort}
	       where id = #{id}  
	</update>
	
	<select id="list" resultType="channelArticlescrap">
	        select 
	          id,
	          articlescrap_id,
	          channel_id,
	          sort
	        from channel_articlescrap 
            where channel_id=#{channel_id}
	</select>
	
	<delete id="deleteById">
	        delete from channel_articlescrap where id =#{id}
	</delete>
	
</mapper>
