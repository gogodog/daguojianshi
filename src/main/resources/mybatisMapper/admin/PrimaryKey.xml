<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dgjs.mapper.admin.PrimaryKeyMapper">
  
  <select id="getPrimaryValue" resultType="long">
     select primary_value from primarykey where id = #{id} for update;
  </select>
  
  <update id="updateValue">
     update primarykey set primary_value = #{primary_value} where id = #{id}
  </update>
</mapper>
